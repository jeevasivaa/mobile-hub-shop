# ─────────────────────────────────────────────────────────
# Mobile Shop — Python Dependencies
# ─────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Template engine
jinja2>=3.1.4

# Database
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9
alembic>=1.13.1

# Validation (use latest for Python 3.14 wheel compatibility)
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Forms & file uploads
python-multipart>=0.0.9
aiofiles>=23.2.1

# Authentication (using bcrypt directly — passlib is incompatible with bcrypt 5.x)
bcrypt>=4.0.0
itsdangerous>=2.2.0

# Environment
python-dotenv>=1.0.1

# Image processing
pillow>=10.0.0

# Testing
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.6
